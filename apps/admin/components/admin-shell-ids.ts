/** Stable DOM ids for admin-shell elements addressed imperatively elsewhere. */

/**
 * The header light/dark theme toggle. The Appearance dialog clicks this by id to
 * preview an animation over the real app (rather than running a full-screen
 * reveal inside a modal, which can wedge Radix's body pointer-events lock).
 */
export const THEME_TOGGLE_ELEMENT_ID = "admin-theme-toggle";
